# CVIS Backend — Python dependencies
# Install with: pip install -r requirements.txt

fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
aiosqlite==0.20.0
python-dotenv==1.0.1
websockets==13.1

# Phase 2 — Auth + Integrity + MQTT
paho-mqtt==2.1.0
cryptography==43.0.3

# Phase 3 — AI (async Ollama client)
httpx==0.27.2

# Phase 5 — Admin (system stats)
psutil==6.1.0
